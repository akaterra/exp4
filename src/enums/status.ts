export enum Status {
  CANCELED = 'canceled',
  COMPLETED = 'completed',
  FAILED = 'failed',
  PENDING = 'pending',
  IN_PROGRESS = 'inProgress',
  QUEUED = 'queued',
  SCHEDULED = 'scheduled',
  SUCCESS = 'success',
  SYNCING = 'syncing',
  UNKNOWN = 'unknown',
}
