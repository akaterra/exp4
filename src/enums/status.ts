export enum Status {
  CANCELED = 'canceled',
  COMPLETED = 'completed',
  FAILED = 'failed',
  PENDING = 'pending',
  PROCESSING = 'processing',
  QUEUED = 'queued',
  SCHEDULED = 'scheduled',
  SUCCESS = 'success',
  SYNCING = 'syncing',
  UNKNOWN = 'unknown',
}
