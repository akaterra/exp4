// import { h, render } from 'preact';
// import './padding.css';

// export const PeddingHor = ({ children, style }: { children, style? }) => {
//     return <div className="padding hor" style={ style }>{ children }</div>;
// };

// export const PeddingVer = ({ children, style }: { children, style? }) => {
//     return <div className="padding ver" style={ style }>{ children }</div>;
// };
