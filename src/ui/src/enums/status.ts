export enum Status {
  COMPLETED = 'completed',
  FAILED = 'failed',
  NOT_STABLE = 'notStable',
  PROCESSING = 'processing',
  SCHEDULED = 'scheduled',
  STABLE = 'stable',
  SUCCESS = 'success',
  UNKNOWN = 'unknown',
}
