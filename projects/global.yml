type: "global"

auth:
  github:
    type: "github"
    config:
      credentials:
        clientId: null
        clientSecret: null
        callbackUrl: null
      integration: "github"
      storage: "github"
  jumpcloud:
    type: "saml2"
    config:
      integration: "jumpcloud"

integrations:
  github:
    type: "github"
    config:
      org: "getpackageltd"

storages:
  github:
    type: "github"
    config:
      integration: "github"
